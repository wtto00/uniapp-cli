import { describe, it } from 'node:test'

describe('harmony', () => {
  it('requirement', { timeout: 10000, todo: true })
  it('add', { timeout: 60000, todo: true })
  it('remove', { timeout: 60000, todo: true })
  it('run', { timeout: 60000, todo: true })
  it('build', { timeout: 60000, todo: true })
})
